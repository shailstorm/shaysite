import { Karla, Lora } from "next/font/google";

export const karla = Karla({
  weight: ['400', '500', '700'],
  subsets: ['latin'],
});

export const lora = Lora({
  weight: ['400', '600'],
  subsets: ['latin']
});